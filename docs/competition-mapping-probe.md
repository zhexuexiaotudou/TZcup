# B-mapping：5 分钟可行性探针

**NO-GO；20000 m² 面积门 FAIL；未启动正式长跑。SIM_RESOURCE_RELEASED=true。**

唯一一次真实 Gazebo + SLAM 探针持续 300.013 秒墙钟，最终场地内已知栅格 560870 个、分辨率约 0.05 m、有效观测面积 **1402.175 m²（7.01%）**。地图尺寸 1056×1162、原点 [-22.882026, -29.008907, 0]，边界面积 3067.680 m² 仅是栅格外框，不能计作观测面积。NPZ 保留原始 -1/0/100 栅格，PGM/YAML 由这一实际地图生成，没有加载旧地图、填补未知格或使用真值建图。

| 实测指标 | 结果 |
|---|---:|
| 实际 RTF | 0.124420 |
| 最后有效 /clock 时刻 | 34.768 s |
| 首末地图间平均增长率 | 5.235 m²/墙钟秒 |
| 后 120 秒窗口增长率 | 2.885 m²/墙钟秒 |
| 后 60 秒窗口增长率 | 1.370 m²/墙钟秒 |
| 按后 120 秒线性外推剩余时间 | 107.5 min |
| 按后 60 秒线性外推剩余时间 | 226.3 min |
| 进程 RSS 求和峰值（共享页可能重复计数） | 5.181 GiB |
| 采样 CPU 使用峰值 | 9.93 核 |
| GPU 采样利用率 / 显存峰值 | 6% / 334 MiB |

早段平均线性外推约 59.2 分钟，但后段增长明显减慢；后 60 秒仅增加 82.04 m²，后 20 秒仅增加 9.36 m²。当前已观察到重叠/局部收益下降，不能证明公共路线后续能持续早段速率。用户未提供明确截止时刻，只要求尽快，因此不声称已严格比较某个已知剩余预算；依据实测后段需约 1.8–3.8 小时且下降的趋势，不启动长跑。上述都是线性估计，非完成保证。候选路线的 20–35 仿真分钟不是实测，本次 NO-GO 不以它为依据。

固定 200×100 m geofence 的统计上限恰为 20000 m²，达到门槛需 100% 已知格。该上限不是可达性证明；本次没有证明全场所有格在遮挡下可观测，也没有把 95%=19000 m² 降格为 PASS。未完成公共路线，不宣称已经实测全场可观测上限或全场饱和。

## 配置与证据

- 在基线 `106f1189a5aacf856412d1c1bc1f76919c26962c` 上仅应用 B，候选提交 `cd4985883a719edbf6c2b93c2dd090cafb580fb6`。此前清扫、定位、感知代码与结果保留。
- 显式 `competition_29m_candidate:=true`、`mission_mode:=mapping`、`operation_speed_profile:=mapping_safe`，默认仍关闭；传感器物理上限 `/scan.range_max=30` m，过滤后最大有限值 29 m，实际 SLAM 参数服务读回 `max_laser_range=29`、`scan_buffer_maximum_scan_distance=29`。原始有限命中、NaN 自遮挡不填充；+Inf 无返回射线按已有扫描策略截至 29 m。
- 3 项 focused pytest 通过，只构建 `sanitation_formal_campus_integration`，构建 rc=0；运行 rc=0。没有重跑定位/感知/清扫、视频、PR 或全量 CI。
- 主观测器 `/clock` 使用 RELIABLE 导致 QoS 不兼容，其 `sim_s=0` 无效；同一次仿真增加 BEST_EFFORT 只读观测器，RTF 来自 `clock_observation.json` 的 29.272 仿真秒区间。原记录保留，不掩盖此采集故障。
- 原始 NPZ 读取后数据逐格一致，PGM 读取后已知格掩码一致，向量化统计与仓库 `assess_grid_observation` 逐格复算一致。此处是**离线文件重载**，没有启动另一轮 live map-server 重定位验收。
- `evidence.tar.gz` 保存真实栅格、面积时序、完整 launch/probe 日志、配置读回、采集源码及哈希；本地核对 31 个文件 SHA-256 全通过。输出目录 `assessment.json` 增补后 60 秒计算，封存包内原 assessment 保持不变。
- domain 92 / partition `tzcup_mapping_probe_20260913_01` 无残留进程，Gazebo 无残留，独占锁可获取。无关 domain 99 旧 bridge 保留。

已用 neat-freak 按本次范围同步专题文档与 README 入口；未修改记忆、未清理分支/worktree/证据。保留候选只供审阅，不代表通过大地图验收；回退点为基线 `106f1189`，默认不启用候选即可保留原行为。
