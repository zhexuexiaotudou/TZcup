# AUTO-10 compact evidence

This directory contains compact pass/fail metrics and hashes of the external 500-audio and 1200-case raw matrices. Audio and case-level files remain outside Git because they are reproducible and large. The browser QA record includes pre-pass defects; it is not a claim that static assertions alone verified rendering.
