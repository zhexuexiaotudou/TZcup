# e999e53 左右转闭包诊断

状态：转向与短程定位诊断通过；正式首图、Golden Mission 和比赛验收尚未通过。

- 候选：`e999e53ca8c3d4bbab761047316078936d5c44f0`。
- Runtime closure：`5fd6937140332c026482576259c310182a1cacd3b5cf132474e2e51dca45bd1e`。
- 完整 MCAP：`bag_0.mcap`，91,920,342 bytes；本地与远端 SHA256 均为 `f462232b610fda4ec20d4817642e0b66999267ef95193b29e71a2b96d0165959`。
- 驱动、录包、回收及外层运行器均返回 0；正常 SIGINT 封口，无强杀录包。
- 656/656 组五流配对，覆盖 65.8 秒仿真时间；最大五流时间跨度 15 ms，未放宽 100 ms 门。
- 左转：14.9 秒窗口，GT 偏航 +0.987918 rad，路径 3.031360 m。
- 右转：14.9 秒窗口，GT 偏航 −0.981599 rad，路径 2.994230 m。
- raw/EKF 与 GT 方向一致，关联了实际安全控制链；原始里程计最大误差 0.939949 m，EKF 最大误差 0.259544 m，均低于 2 m。
- 急停反馈为 true 后的前 30 秒墙钟时间内：174 个 GT 样本保持静止；34 条四轮力矩状态共 136 个值均为零。触发前已有停稳段，因此这不是行驶中急停制动验收。
- 四轮速度、力矩为原始运行数据；接触传感器数据缺失。轮缘与 GT 运动学残差仅为 `DERIVED_PROXY`。

`mobility-turn-e999e53-01` 因诊断运行器误关仿真安全输入而启动失败，未执行转向阶段；原始失败和正常关闭的录包保留，不从失败统计中删除。此前 38dccd4 的真实转向失败同样保留。

## 首图阻断及待批准修复

`first-map-e999e53-01` 因缺少 ROS2 可执行文件绑定在启动前失败；补齐该绑定后的 `first-map-e999e53-02` 又被 canonical snapshot 库存检查拒绝。两次均未启动正式首图仿真，不计作首图通过。

快照生成器已在独立工作树重生成六个产物并通过 `--check`，实际差异仅三个生成文件：URDF、组件报告、快照清单。补丁 SHA256 为 `2133a20c93053756d622a037d8ad2dbf9157b1d7a5fa43dfcc8629d7b5667604`；算法、脚本、配置和 runtime 源码未改。尚未提交或提升为唯一继任候选。

下一步：确认该生成物补丁的候选切换；复验原 runtime 的完整闭包；新建 acceptance session 与运行绑定后启动首图。禁止以现有诊断报告替代正式首图 manifest/receipt，也不自动宣告 Golden Mission 完成。
