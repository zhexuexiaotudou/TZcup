# Preserve copied evidence bytes, including original line endings and trailing spaces.
evidence/** -text

# Preserve packaged video deliverables byte-for-byte, including sidecar text assets.
video/** -text

# Preserve the submitted source bundle byte-for-byte so CODE_MANIFEST.sha256 stays valid.
code/** -text
