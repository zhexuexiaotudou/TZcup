# 验证记录

截至2026-09-14：`py -3 tools/refresh.py` 成功，实际生成22页PDF，不硬编码页面数。结果源`results.json`包含13项（PASS 4、FAIL 5、PARTIAL 1、NOT_MEASURED 3），唯一矩阵、CSV、Markdown和PDF由同一次刷新生成。

已验证：

- `py -3 -m json.tool`：`results.json`、`sections.json`、`evidence/day1/evidence-index.json`、`evidence/index.json`均通过。
- `pdfinfo`：PDF 22页，A4 595.28 x 841.89 pts。
- `pdftoppm -png -r 110`：22页全部渲染，联系表目视检查无裁切、重叠、乱码或空白页。
- PDF SHA-256：`250C8DEB1E2429186D06CA7C6ABA6C5BEFA63426DD729D0DD75E0E783EDA333A`。
- `git diff --check`：通过。
- 未运行ROS、Gazebo、ADB、远端命令、构建或长测试；未新增测量值。
