# 验证记录

截至2026-09-14：`py -3 tools/refresh.py` 成功，实际重建23页PDF，不硬编码页面数。结果源`results.json`包含14项，状态为PASS 5、FAIL 5、PARTIAL 2、NOT_MEASURED 2；唯一矩阵、CSV、Markdown和PDF由同一次刷新生成。感知仅保留Stage5B raw 0/654/76与Stage5A raw 33/50/43两套受控域诊断，policy为`NOT_RUN/NOT_MEASURED`；官方识别状态固定为`NOT_MEASURED`，工程感知仅按已测raw证据为`FAIL`。急停预先约定保持判据为1.0s、实际观测保持1.5s，0.489s PASS不变。

## 封包检查

- `py -3 -m json.tool`：`metrics/results.json`、`docs/sections.json`、`evidence/day1/evidence-index.json`、`evidence/index.json`、`raw-evidence-sources.json`、`competition-perception-replay-status.json`、`demo-failure-retention.json`及全部day1结果JSON通过。
- JSON/path/schema复核：14项结果，全部结果证据路径存在；状态统计为`PASS=5, FAIL=5, PARTIAL=2, NOT_MEASURED=2`。
- 原始证据定位复核：宽度、定位、感知的源目录、文件大小和SHA-256逐项核对；`3ae009a`的`replay_status.json`包内副本为3,177字节且SHA-256完全一致，policy为`NOT_RUN`。宽度的1.0s预声明判据、1.5s观测保持和定位/感知源绑定均可追溯到实际存在的运行根。
- Demo失败留存：`demo-failure-retention.json`绑定源提交`12c2a6a`、两次失败目录和紧凑日志哈希；不虚报MP4或MCAP。
- `pdfinfo`：PDF 23页，A4 595.28 x 841.89 pts。
- `pdftoppm -png -r 110`：23页全部渲染；两张联系表覆盖全部23页，并对修改页和首尾页原图检查，无裁切、重叠、乱码或空白页。
- `pdftotext -enc UTF-8`：未发现U+FFFD替换字符；`pdffonts`显示SimSun子集已嵌入。
- PDF SHA-256：`73469E0B672D9981CE53CDABBFE995ECDB44E38AA0DABA0CC0E2D3542A05A7E0`。
- `git diff --check`：通过。
- 未运行ROS、Gazebo、S100P、ADB、远端命令或长构建；未新增测量值。

## 新增证据原始测试记录

- 任务分解：focused HMI/AUTO-10/evaluation pytest `29 passed in 0.42s`；legacy 1200-case DSL matrix的semantic/tool/argument accuracy为`1.0`、ambiguity fail-closed `1.0`、unsafe execution `0`、direct actuator access `0`。`ci_fast`因既有README长度181>180在测试前BLOCKED。
- 定位诊断：聚焦测试`44 passed`，Python编译和`git diff --check`通过；排除一个无关既有fixture `NameError`后，更广定位/campus测试`290 passed, 2 skipped`。
- 板端证据：新鲜控制回放`witness_rc=0`、`61`个输出，正向限幅、急停归零、恢复后反向限幅和输出变化全部通过；mapping/localization为synthetic replay通过，planning为有限通过，perception因缺少正式nash-m HBM而BLOCKED。
- 尘箱设计：参数化模型和封包验证通过；9项基线装配净空检查通过，补充包PDF为19页。
- Demo证据：两次Gazebo启动均在ROS daemon阶段失败且Gazebo未启动；没有MCAP、MP4、RViz或dashboard替代，视频保持NOT_MEASURED。
