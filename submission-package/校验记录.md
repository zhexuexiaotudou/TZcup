# 验证记录

截至2026-09-14：`py -3 tools/refresh.py` 成功，实际重建23页PDF，不硬编码页面数。结果源`results.json`包含14项，状态为PASS 5、FAIL 5、PARTIAL 2、NOT_MEASURED 2；唯一矩阵、CSV、Markdown和PDF由同一次刷新生成。感知仅保留Stage5B raw 0/654/76与Stage5A raw 33/50/43两套受控域诊断，policy为`NOT_RUN/NOT_MEASURED`；官方识别状态固定为`NOT_MEASURED`，工程感知仅按已测raw证据为`FAIL`。急停预先约定保持判据为1.0s、实际观测保持1.5s，0.489s PASS不变。Demo已有真实Gazebo GUI partial clip，但没有有效5-10分钟完整任务视频；task、cleaning、coverage和可用MCAP证据仍不存在。

## 封包检查

- `py -3 -m json.tool`：包内25个JSON全部通过。
- JSON/path/schema复核：14项结果，全部结果证据路径存在；状态统计为`PASS=5, FAIL=5, PARTIAL=2, NOT_MEASURED=2`。
- 原始证据定位复核：宽度、定位、感知的源目录、文件大小和SHA-256逐项核对；`3ae009a`的`replay_status.json`包内副本为3,177字节且SHA-256完全一致，policy为`NOT_RUN`。宽度的1.0s预声明判据、1.5s观测保持和定位/感知源绑定均可追溯到实际存在的运行根。
- Demo失败留存：`demo-failure-retention.json`同时保留两次预Gazebo启动失败和两次有界full-task失败。后两次分别绑定运行源提交`476525f`、`022714a`及结果提交`42dcd85`、`56d1689`；均未启动task，也未用RViz/dashboard替代。
- 包内Demo局部证据：6.333333s主clip为95帧、7237 bytes、SHA-256 `a00efd0cb447533573edc463ed1c1d0667dc3bafdcdcc8591438c4e18b837676`，代表PNG为94265 bytes、SHA-256 `1ba4eea2aa263fcf2c3ce0ec1405e456fe028bbf183b8a758b602c643e657034`；2.866667s后续失败attempt clip为43帧、4247 bytes、SHA-256 `04fdba5022fc7cc76740f176fbb5279b711db9863310be4217c974b9a98fb836`，代表PNG为94338 bytes、SHA-256 `229223b98d4e25dbb5949075aebac11d5da73ebefc683e86eb7fee33c242262d`。
- MCAP边界：两次full-task attempt各有1301-byte MCAP容器，但均为0消息、0 duration；没有可用任务MCAP证据。`56d1689` heartbeat hardening未获授权重跑Gazebo。
- `pdfinfo`：PDF 23页，A4 595.28 x 841.89 pts，188751 bytes。
- `pdftoppm -png -r 110`：23页全部渲染；两张联系表覆盖全部23页，并对修改页和首尾页原图检查，无裁切、重叠、乱码或空白页。
- `pdftotext -enc UTF-8`：未发现U+FFFD替换字符；`pdffonts`显示SimSun子集已嵌入。Poppler输出一条`ArialUnicode` display-font解析提示，但PDF字符串中无该字体引用，逐页文本与图像检查均正常。
- PDF SHA-256：`c56dd014b0c1608ca4e26354e667c0c06884384bafbf271bdafa8d83deedff44`。
- `git diff --check`：通过。
- 未运行ROS、Gazebo、S100P、ADB、远端命令或长构建；未新增测量值。

## 新增证据原始测试记录

- 任务分解：focused HMI/AUTO-10/evaluation pytest `29 passed in 0.42s`；legacy 1200-case DSL matrix的semantic/tool/argument accuracy为`1.0`、ambiguity fail-closed `1.0`、unsafe execution `0`、direct actuator access `0`。`ci_fast`因既有README长度181>180在测试前BLOCKED。
- 定位诊断：聚焦测试`44 passed`，Python编译和`git diff --check`通过；排除一个无关既有fixture `NameError`后，更广定位/campus测试`290 passed, 2 skipped`。
- 板端证据：新鲜控制回放`witness_rc=0`、`61`个输出，正向限幅、急停归零、恢复后反向限幅和输出变化全部通过；mapping/localization为synthetic replay通过，planning为有限通过，perception因缺少正式nash-m HBM而BLOCKED。
- 尘箱设计：参数化模型和封包验证通过；9项基线装配净空检查通过，补充包PDF为19页。
- Demo证据：两次预Gazebo启动失败和两次有界full-task失败均保留；后两次有真实nonblack GUI clip，但在task启动前停止，未产生task、cleaning、coverage或可用MCAP结果，也未达到5-10分钟完整视频要求。
