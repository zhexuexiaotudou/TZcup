## 改动与原因

<!-- 说明改了什么、为什么改，以及用户或开发者会受到什么影响。 -->

## 验收证据

- [ ] 已核实问题与验收条件
- [ ] 使用独立分支和独立 worktree，未夹带其他任务改动
- [ ] `python scripts/ci_fast.py` 或 `py scripts/ci_fast.py` 通过
- [ ] 已运行受影响的 pytest / Stage Docker / 页面或真实运行检查
- [ ] 文档、接口、命令和评测口径已同步
- [ ] 未提交密钥、服务器凭据、令牌或任务外文件

验收命令与结果：

```text
待填写
```

## 部署、线上验收与回滚

- 部署目标或 `not_applicable`：
- 部署提交：
- 真实效果检查：
- 回滚点与命令：

## 合并后收尾

- [ ] CI 全绿后合并并确认远端 `main`
- [ ] 已运行 `neat-freak`（`/neat`）完成知识与规则同步
- [ ] 已汇报并等待用户确认；确认前不清理分支、worktree或任务专用临时数据
